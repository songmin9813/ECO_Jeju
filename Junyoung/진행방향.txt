Waste 
city -> 3개의 수준의 팩터형으로 나눈다
em_area_cd -> 배출거점지역 코드 --> 삭제
emd_cd -> 행정동 코드 
emd_nm 행정동 한글명 -> 1234의 수치형변수로 변경
em_g -> 타겟변수

1. 행정동코드와 행정동한글명의 총합 개수는 같지만 행정동코드로 봤을 때와 한글명으로 봤을 때의 개수 차이가 존재함 but  알수없음은 똑같음
--> 행정동한글명으로 unique 키로 설정하여 수치형 변수로 변환 후 에 회귀분석을 진행할 필요성이 보임

2. 숫자형 변수들의 단위가 건, g, 원으로 다 다르므로 정규화 진행할 필요가 있어보임

18번 알수없음

Korean

time -> 일별이므로 합친다?
city -> 0~1개의 팩터형으로 나눈다.
emd_cd, emd_nm -> 두개의 컬럼이 일치하는 것을 확인 하나를 버리고 유니크밸류로 사용해도 될 것 같다.
sex -> 0~1 수준의 팩터형으로 변경
resd -> 0~1 수준의 팩터형
		
* 거주인구 : 1~24시 해당 시간 정각 측정 인구(거주지에 머문 시간(분)/60분)		
* 근무인구 : 1~24시 해당 시간 정각 측정 인구(근무지에 머문 시간(분)/60분)		
* 방문인구 : 1~24시 해당 시간 정각 측정 인구(거주지/근무지 외 지역에 머문 시간(분)/60분)		

1. 일자형으로 만들기 위해서 time 변수를 어떻게 처리할껀지 고민해봐야할꺼같음
2. 거주인구, 근무인구, 방문인구를 합쳐서 할지 평균으로 할껀지도 생각해봐야할 꺼 같다.


Long

nationality -> 수치형변수로 변환
city -> 0~1 팩터형
emd_cd, emd_nm -> 두개의 컬럼이 일치하는 것을 확인 하나를 버리고 유니크밸류로 사용해도 될 것 같다.

* 장기체류 : 90일 이상		
* 거주인구 : 1~24시 해당 시간 정각 측정 인구(거주지에 머문 시간(분)/60분)		
* 근무인구 : 1~24시 해당 시간 정각 측정 인구(근무지에 머문 시간(분)/60분)		
* 방문인구 : 1~24시 해당 시간 정각 측정 인구(거주지/근무지 외 지역에 머문 시간(분)/60분)		

Short

nationality -> 수치형변수로 변환
city -> 0~1 팩터형
emd_cd, emd_nm -> 두개의 컬럼이 일치하는 것을 확인

* 단기체류 : 90일 이내		
* 거주인구 : 1~24시 해당 시간 정각 측정 인구(거주지에 머문 시간(분)/60분)		
* 근무인구 : 1~24시 해당 시간 정각 측정 인구(근무지에 머문 시간(분)/60분)		
* 방문인구 : 1~24시 해당 시간 정각 측정 인구(거주지/근무지 외 지역에 머문 시간(분)/60분)		


Resident
city -> 0~1 팩터형
emd_cd, emd_nm -> 읍면동 전부 84개로 일치
sex -> 0~1 팩터형

* 행정동별 총 거주인구: 주민등록 거주인구 + 외국인 거주인구


Card
city -> 0~1 팩터형
emd_cd, emd_nm -> 알수없음의 value 값이 가장 많음 유니크밸류가능
mct_cat_cd, mct_cat_nm -> 유니크밸류 가능

* 지역 및 카드범위 : 제주도 內 행정동, 신용/체크(개인카드 限)		
* 건수, 금액은 보정 금액		
* 제주도민(자택주소:제주) 기준		
